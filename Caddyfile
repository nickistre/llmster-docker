{
    email {$ACME_EMAIL}
}

{$PROXY_HOST} {
    tls {
        dns cloudflare {$CLOUDFLARE_API_TOKEN}
    }

    @unauthorized not header Authorization "Bearer {$LLM_API_KEY}"
    respond @unauthorized "Unauthorized" 401

    reverse_proxy llmster:{$LLMSTER_PORT:1234}
}
